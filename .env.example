# Backend Environment Variables
# Copy this file to .env for local development

# Pinata API JWT for IPFS storage (REQUIRED)
PINATA_JWT=your_pinata_jwt_here

# Node environment (development or production)
NODE_ENV=development

# Server port (optional, defaults to 3000)
PORT=3000

# Optional: Custom Pinata gateway
# PINATA_GATEWAY=your-gateway.mypinata.cloud

# For Northflank deployment - URL of the frontend for CORS
# FRONTEND_URL=https://your-frontend-url.vercel.app

# Frontend Environment Variables
# These should be set in .env.local for the frontend or in Vercel environment variables

# API URL for the backend server (REQUIRED for frontend)
# VITE_API_URL=http://localhost:3000
